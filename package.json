{
  "name": "operit-acp-plugin",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Operit ACP context-compression ToolPkg (billion-context kernel port): progressive, reversible, in-pipeline compression.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "package": "node scripts/package.mjs",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test tests/*.test.mts",
    "verify": "npm run typecheck && npm test && npm run build && npm run package"
  },
  "dependencies": {
    "acp-kernel": "0.0.54"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  },
  "allowScripts": {
    "esbuild@0.24.2": true,
    "esbuild@0.28.2": true
  }
}
