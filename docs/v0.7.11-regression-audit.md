# V0.7.11 V0.7.5→V0.7.6 Context 30万→80万跳变回归取证报告

日期：2026-09-09
方式：只调查不修复（runtime evidence 取证）
基线：V0.7.10 调查结论 + 本报告

---

## 1. EXECUTIVE VERDICT

**V0.7.5 的 preflight 在 Operit token estimator 之前把 80万级 history 压缩到 ~15万，是"以前不跳变"的原因；V0.7.6 删除 preflight 后全量 255万直接进入 estimator，导致 30→80万跳变。这不是 Operit stale 单因，而是 V0.7.6 行为回归 + Operit stale display 的复合问题。**

## 2. VERSION DIFFERENTIAL

| 行为 | V0.7.5 | V0.7.6 | V0.7.10(now) |
|---|---|---|---|
| hard 触发 | preflight auto-fold | forced nudge | forced nudge |
| 投影 | 大幅压缩(proj=132~292) | 不压缩(proj=raw+1) | 不压缩(proj=raw+1) |
| 进 estimator history | ~15万 | ~255万 | ~255万 |
| UI 跳变 | 不跳(一直~15万) | 跳(30→80万) | 跳 |

## 3. TIMELINE（同 chat d2572685）

```
T0 (静止 UI)             ≈ 30万 (DB currentWindowSize stale)
T1 (hard 前 estimate)    V0.7.5: 56~81万  /  V0.7.6: 251~260万
T2 (hard 后 projection)  V0.7.5: 8.7~14.8万  /  V0.7.6: 无变化
T3 (calculateInputTokens) V0.7.5: ~15万  /  V0.7.6: ~255万
T4 (provider actual)     90.5万 (双方相同，因 cached 计费)
```

## 4. TOKEN TABLE

| 指标 | V0.7.5 | V0.7.6+ |
|---|---|---|
| raw turns | 2792/3045/3182 | 3814/3825/3881/4017/4045/4081 |
| preflight freed | 487K/629K/641K tokens | N/A |
| project turns | 132/176/292 | raw+1 |
| estimate (T2) | 86,837/111,733/148,246 | 2,515,332~2,597,542 |
| effPct | 43/56/74% | 1258~1299% |

## 5. HISTORY TRANSFORMATION

```
RAW(80万级) 
  → V0.7.5: preflight auto-fold(压 48-64万) → PROJECT(~15万) → requestHistory(~15万) → estimator(15万)
  → V0.7.6: forced nudge(不压) → PROJECT(raw+1) → requestHistory(全量) → estimator(~255万)
```

## 6. HARD THRESHOLD TRACE（runtime 实测）

```
V0.7.5: t=1788865346 context=811,214 hard=170,000 → preflight freed=641,329 → proj=292 → est=148,246
V0.7.6: t=1788889311 raw=4081 proj=4082 tok=2,597,542 effPct=1299 forced（无压缩）
```

## 7. REGRESSION ANALYSIS

**V0.7.6 引入行为回归 = CONFIRMED**
- 删除 preflight 后，hard threshold 不再保护 estimator 输入规模
- 从"hard 时压到 15万"变为"hard 时全量 255万"

## 8. V0.7.10 REPORT CORRECTION

| V0.7.10 结论 | 修正 |
|---|---|
| 30万 = stale DB estimate | ✅ 仍成立 |
| 80万 = 当前请求 estimate | ✅ 成立，但需补充: estimate 对象是 255万(ACP 视图) vs 80万(宿主视图) |
| ACP_RESPONSIBLE = NO | ⚠️ 修改为 ACP_DIRECT_SIZE_INFLATION = NO + ACP_REGRESSION_CONTRIBUTION = CONFIRMED |
| OPERIT_RESPONSIBLE = YES | ✅ 成立（stale display），但根因是 ACP 回归 + Operit stale 复合 |

## 9. ROOT CAUSE CLASSIFICATION

**ACP + OPERIT**（复合）
- ACP：V0.7.6 删除 preflight 保护（回归）
- Operit：currentWindowSize stale 显示放大了感知

## 10. 结论等级

**A = V0.7.5 preflight 明确阻止了 80万进入 request**（runtime 证据）
