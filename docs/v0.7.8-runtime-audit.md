# V0.7.8 Runtime Compression Continuity Audit Report

日期：2026-09-08
提交基座：a96f90c（V0.7.8）
方式：只审计不改代码（AUDIT ONLY）

---

## 1. Runtime Compression Event

- 时间：22:18:19（t=1788877099）
- 来源：模型主动 compress（`source=model`）
- 输入 refs：`m03000..m03297`（ACP session-local ref）
- 结果：1 block 创建
- 释放：**141,652 tokens**

## 2. Compression Block

```
blockId: b45, tier: 1, active: true
directMessageIds: 192, effectiveMessageIds: 294
```

## 3. Covered CoreMessage IDs

192 条，全部为 `kind||contentHash` 格式（legacy content-derived identity）：
```
TOOL_CALL||5ad4b529e9443f4f
TOOL_RESULT||45b04ce5c97e2ee3
ASSISTANT||5a8260d3dd3676b1
...
```

## 4. ACP Ref Mapping

```
m03102 -> TOOL_CALL||5ad4...
m03103 -> TOOL_RESULT||45b0...
m03104 -> TOOL_CALL||b28a...
...
```

## 5-7. Next-Hop Identity & Representation

- 下一 Hop（22:32:21, t=1788877941）：`raw=3825 proj=1625 blocks=44→45`
- **b45 covered 192 条 → 投影中仍存在: 0 → 被 SUMMARY 替代: 192 (100%)**
- 45 blocks → 43 SUMMARY 占位（投影端）
- **COMPRESSION CONTINUITY = PASS**（覆盖对象 100% 被 SUMMARY 正确替代）

## 8. Token Release Accounting

```
kernel compression credit: 141,652（usageState.compressionCreditTokens）
modelSavedTokens 累计: 253,799
host currentWindowSize: 338,371
ACP estimate(trace): 2,518,289
投影: 3825 turns → 1625 turns
```
→ **141,652 是 kernel credit（B 类）**，非 host window 直接减少，但投影真实折叠 57%。

## 9-10. Compress Tool Path / Host Nudge Ref Origin

- compress 路径直接调 `promptTurnsToCoreMessages(rawTurns)`（**不带 identity 参数**）→ 回退 content-hash
- **mapping 中 host:* id = 0/3825** → identity-bridge 未在 compress 路径生效
- 宿主 nudge ref（m05709..m06088）为宿主独立视图，与 ACP session ref（m03297）不同体系

## 11. Ref Taxonomy

| Identifier | Owner | Scope | Cross-Hop | Purpose |
|---|---|---|---|---|
| Host message id | Operit | chat | 无 | 不存在（PromptTurn 无 id） |
| CoreMessage.id | ACP adapter | session | content-hash | kernel identity |
| mxxxxx | ACP kernel | session | 稳定 | model-visible ref |
| blockId | ACP | session | 稳定 | compression state |
| SUMMARY id | ACP projection | session | 每轮重算 | projected representation |

## 12. Verdicts

```
IDENTITY_BRIDGE_RUNTIME = NOT_VERIFIED（compress 路径未接入 identity 参数）
COMPRESSION_CONTINUITY = PASS
HOST_REF_AND_ACP_REF_ARE_SAME_SYSTEM = NO
HOST_REF_TO_ACP_REF_TRANSLATION_REQUIRED = NOT_PROVEN（暂不需要）
COMPRESS_PATH_USES_IDENTITY_BRIDGE = NO
KERNEL_CHANGE_REQUIRED = NO
AUTO_FOLD_REINTRODUCED = NO
```

## 13. Required Changes For V0.7.9

1. **compress 工具路径接入 identity-bridge**：`applyCompression` 调 `promptTurnsToCoreMessages(rawTurns, identity)`，使 block covered id 使用 bridge id（与 project 一致）
2. 之后重新验证 b45 covered 192 条在下一 Hop 的 continuity（应保持 100% SUMMARY 替代）
