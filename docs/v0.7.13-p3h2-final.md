# V0.7.13-P3-H-2 FINAL：插件 JS → 宿主 summary 写入通道审计

日期：2026-09-09
状态：取证完成（未改任何代码）

---

## 最终结论（三选一）

```
结论 C：完全没有，需要新增最小桥接
```

## 一、宿主→插件（通知）通道（存在）

- `ToolPkgPromptHookBridge`：registerInput/History/EstimateHistory/SystemPromptCompose/
  ToolPromptCompose/Finalize/EstimateFinalize 7 类钩子（全部宿主→插件回调）
- `ToolPkgChatMessageHookBridge.dispatchMessagePersisted(targetChatId, message)`：
  消息持久化通知（宿主→插件）
- 插件 JS 可用：register*Hook / ToolPkg.ipc / getConfigDir（main.ts 证实）

## 二、插件→宿主（写入）通道（不存在）

- `ChatHistoryManager.addSummaryMessageBetweenSliceNeighbors`（Kotlin 内部单例）
- `ChatHistoryDelegate.addSummaryMessage`（Kotlin 内部）
- **JS 侧无任何 ChatHistory / Message / Summary 写入 API**
- 无 deleteMessagesFrom / messageDao 暴露（好事，符合安全约束）

## 三、最小桥接设计（待批准，本轮未改）

```kotlin
// 新增：插件→宿主 summary 提交（唯一入口，不暴露删除/DAO）
fun commitAcpSummary(
    chatId: String,
    summaryMessage: ChatMessage,   // sender="summary"
    beforeTimestamp: Long,
    afterTimestamp: Long
) {
    // 内部调 ChatHistoryDelegate.addSummaryMessage / 
    //   ChatHistoryManager.addSummaryMessageBetweenSliceNeighbors(...)
}
```

暴露给 JS：`ToolPkg.chatHistory.commitAcpSummary(chatId, message, beforeTs, afterTs)`

**不暴露**：deleteMessagesFrom / messageDao.* / 通用 updateMessage

## 四、ACP 复用路径（目标态）

```
ACP compress 建块
  → 生成 sender="summary" 摘要消息
  → ToolPkg.chatHistory.commitAcpSummary(chatId, summaryMsg, beforeTs, afterTs)
  → 宿主写入 DB（官方接口）
  → 后续 getMemoryFromMessages 自动只发 summary + 新消息
  → 旧消息保留 DB，永不发送 ✅
```

## 五、阻塞项

- 需要宿主侧新增 commitAcpSummary 桥（Kotlin 改动 + JS API 暴露）
- 属于宿主改动，需用户批准后再实施